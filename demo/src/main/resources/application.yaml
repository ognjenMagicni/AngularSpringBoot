spring:
  application:
    name: FullStackApp
  datasource:
    url: jdbc:mysql://localhost:3306/posao
    username: root
    password: simple
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true